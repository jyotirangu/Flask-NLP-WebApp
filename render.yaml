services:
  - type: web
    name: flask-nlp-webapp
    env: python
    rootDir: Flask_backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9